extends ../layout

block content

   h2(class='ls-title') Authentication with PKI Express

   h5
      | Authentication failed&nbsp;
      i(class='fas fa-times-circle', style='color: red;')

   p !{vrHtml}

   p
      a(href='/authentication-restpki' class='btn btn-primary') Try Again